// import body from "../body/Body"
 function Isai(props) {
    return (
      <div>
          <img src={props.url} alt={props.altkey} />
          <p>{props.name}</p>
      </div>
    );
  }
export default Isai;
